<!DOCTYPE html>
<html lang="en">
    {% include('/shared/beforescript.html.twig') %}
    {% include('/shared/head.html.twig') %}
    {% if title == "Staff Login" %}
        <link rel="stylesheet" href="/assets/stylesheets/signin.css"/>
    {% endif %}
    <body>
        <header class="header">
            {% if title != "Staff Login" %}
                <nav class="navbar navbar-expand-lg navbar-light bg-transparent">
                    <ul class="navbar-nav ml-auto">
                        <button type='button' class='btn btn-primary btn-block text-uppercase font-weight-bold'
                                onclick="window.location.href = '/staff/logout';">Logout
                        </button>
                    </ul>
                </nav>
            {% endif %}
        </header>
        {% block content %}{% endblock %}
    </body>
    {% include('/shared/afterscript.html.twig') %}
</html>
